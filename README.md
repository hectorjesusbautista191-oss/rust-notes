# rust-notes
Este repositorio tiene notas de clases de programacion en lenguaje Rust
## Se agregan manuales y ejercios
### gits(solo se usan para github)
-stage(copia de seguridad para guardar los cambios,o recpilador de lo que subiere github) 
- git init(para crear un repositorio local)
#### PASOS PARA SUBIR A GITHUB
- git add(agrega los cambios al area de stage):si utilizo git_add_.,paso los datos al stage
- git_status(detecta los cambios en el repositorio)
- git_commit_m_"mensaje"(prepara el git push para mandarlo a la nube)
- git_push(manda directo a la nube o git hub):
- listo ya subiste tu archivo a "git"
